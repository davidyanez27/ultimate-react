import { LoginForm } from "../../Components/Auth/LoginForm"
import { AuthLayout } from "../../Layouts"

export const LoginPage = () => {
  return (
    <AuthLayout>
      <LoginForm/>
    </AuthLayout>
  )
}
